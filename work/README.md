# Local source-material layout

Create these directories locally; their downloaded contents are ignored by Git:

```text
work/
├── code_audit/          # authors' repository at the required commit
└── replication/
    └── Data/            # extracted Dryad Data directory
```

Do not commit participant data or pretrained checkpoints to this repository.
